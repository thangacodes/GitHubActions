name: "Terraform action"

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  id-token: write # Required for AWS OIDC connection
  contents: read # Required for actions/checkout
  pull-requests: write # Required for GitHub bot to comment on PR

env:
  TF_LOG: INFO
  AWS_REGION: ${{ secrets.TF_AWS_REGION }}
  TF_AWS_BUCKET_NAME: ${{ secrets.TF_AWS_BUCKET_NAME }}
  TF_AWS_BUCKET_KEY_NAME: ${{ secrets.TF_AWS_BUCKET_KEY_NAME }}

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: ${{ secrets.TF_AWS_ROLE }}
          aws-region: ${{ secrets.TF_AWS_REGION }}
          role-session-name: GitHub-OIDC-TERRAFORM

      - name: Run Terraform
        run: |
          cd terra-codes
          terraform init -backend-config="bucket=${{ secrets.TF_AWS_BUCKET_NAME }}" \
                         -backend-config="key=${{ secrets.TF_AWS_BUCKET_KEY_NAME }}" \
                         -backend-config="region=${{ secrets.TF_AWS_REGION }}"
          terraform fmt
          terraform validate
          terraform plan
        env:
          TF_LOG: INFO
          AWS_REGION: ${{ secrets.TF_AWS_REGION }}
